export interface PostAttributes {
  title: string;
  slug: string;
  description: string;
  coverImage: string;
}

export interface LectureAttributes {
  title: string;
  description: string;
  week: number;
  date: string;
}
